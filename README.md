# Quizzical

Quick start:

```shell
npm install
npm run build
open index.html
````

## Development

Run Webpack in watch-mode to continually compile the JavaScript as you work:

```shell
npm run watch
```
